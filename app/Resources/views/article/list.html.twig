{% extends "base.html.twig" %}

{% block title %} Article {% endblock %}

{% block body %}
    <h1>Article</h1>
    {% for article in articles %}
        <h2>{{ article.title }}</h2>
        <div>
            {{article.content}}
        </div>
    {% endfor %}
{% endblock %}